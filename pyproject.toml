[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "network-segmentation-audit"
version = "0.1.0"
description = "Offline defensive network segmentation policy analysis"
requires-python = ">=3.11"
license = {text = "MIT"}

[tool.setuptools.packages.find]
include = ["segmentation_audit*"]
